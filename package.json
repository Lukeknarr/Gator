{
  "name": "gator-project",
  "version": "1.0.0",
  "description": "Gator - Personalized Media Discovery Engine",
  "private": true,
  "scripts": {
    "build": "cd frontend && npm install && npm run build",
    "dev": "cd frontend && npm run dev",
    "start": "cd frontend && npm run start"
  },
  "engines": {
    "node": ">=18.0.0"
  }
} 